# Core module pending

# --- VS Code Context Awareness ---
# COCKPIT_VSCODE_PROFILE が設定されていない場合の初期値
export COCKPIT_VSCODE_PROFILE="${COCKPIT_VSCODE_PROFILE:-}"

# --- VS Code Context Awareness ---
# COCKPIT_VSCODE_PROFILE が設定されていない場合の初期値
export COCKPIT_VSCODE_PROFILE="${COCKPIT_VSCODE_PROFILE:-}"
